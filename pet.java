public class Pet {
    public static void main(String[]args) {
        
    // name (PRIVATE) 
    // age (PRIVATE) 
    // location (PRIVATE) 
    // type (PRIVATE) 

Pet new_pet = new Pet (); }
    private new_pet.name Chipper;
    private new_pet.age = 3;
    private new_pet.location = London
    
    // two constructors(empty, all attributes)
    public Pet(String first, String last);
    {
     firstName = first;
     lastName = last;
    }
    public Pet(String first, String last);
    {
     firstName = first;
     lastName = last;
    }
    // Code to be able to access the following (Get Methods): 

    // name, age, type 

    // Code to be able to change (Set Methods): 

    // name, age, location    
{   
}
   }

